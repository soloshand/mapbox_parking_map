import firebase from 'firebase/app';
import 'firebase/database';

const fireConfig = {
  //firebase_connection_configuration
}

firebase.initializeApp(fireConfig);

export default firebase;
